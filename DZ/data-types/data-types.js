const myName = 'Егор';
console.log('myName', myName, typeof myName);

const myAge = 26;
console.log('myAge', myAge, typeof myAge);

const imHuman = true;
console.log('imHuman', imHuman, typeof imHuman);

const myAverageSleepValue = null;
console.log('myAverageSleepValue', myAverageSleepValue, typeof myAverageSleepValue);

let normalizingMySleep = undefined;
console.log('normalizingMySleep', normalizingMySleep, typeof normalizingMySleep);

const im = {
    myName : 'Егор',
    myAge : 26,
    imHuman : true,
};
console.log('im', im, typeof im);

const id = Symbol('id');
console.log('id', id, typeof id);

const someNumber = 100500n;
console.log('someNumber', someNumber, typeof someNumber);